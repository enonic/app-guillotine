query {
    guillotine {
        getPermissions(key: "contentId") {
            inheritsPermissions
            permissions {
                principal {
                    value
                }
                allow
                deny
            }
        }
    }
}
