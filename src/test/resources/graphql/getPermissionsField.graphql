query {
    guillotine {
        getPermissions(key: "contentId") {
            permissions {
                principal {
                    value
                }
                allow
                deny
            }
        }
    }
}
