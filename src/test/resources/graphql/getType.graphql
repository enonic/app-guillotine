query {
    guillotine {
        getType(name: "portal:site") {
            name
            title
            description
            superType
            abstract
            final
            allowChildContent
            contentDisplayNameScript
            form {
                formItemType
                name
                label
            }
            formAsJson
        }
    }
}
