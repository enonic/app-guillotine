query {
    guillotine {
        attachmentUrl: get(key: "contentid") {
            ... on media_Image {
                mediaUrl(params: {
                    a: 1,
                    b: [2, 3],
                    c: []
                }) {
                    url
                    path
                    queryString
                    intent
                }
            }
        }
        downloadAttachmentUrl: get(key: "contentid") {
            ... on media_Image {
                mediaUrl(download: true) {
                    url
                    intent
                }
            }
        }
    }
}
