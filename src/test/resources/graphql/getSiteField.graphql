query {
    getSite: guillotine {
        getSite {
            _id
            displayName
        }
    }
    getSiteByKey: guillotine(siteKey: "/siteKey") {
        getSite {
            _id
            displayName
        }
    }
}
